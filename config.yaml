model:
  gradient_refinement:
    base_step_size: 0.001
    enabled: false
    max_iterations: 10
  name: multi_scale_conv1d
signal:
  length: 1024
  sampling_rate: 1024
  add_complex_signal: false
training:
  batch_size: 32
  early_stopping:
    min_delta: 0.0001
    patience: 2000
  learning_rate: 0.0001
  num_epochs: 2000
loss:
  name: gradient_aware
  position_weight: 10.0
  magnitude_weight: 0.04
  gradient_weight: 0.000002
  second_derivative_weight: 0.000005
visualization:
  num_predictions: 10
  plot_frequency: 100  # Set to null to only plot final loss figure, or an integer for periodic plotting
