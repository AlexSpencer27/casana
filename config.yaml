model:
  gradient_refinement:
    base_step_size: 0.001
    enabled: false
    max_iterations: 10
  name: multi_scale_conv1d
signal:
  length: 1024
  sampling_rate: 512
  add_complex_signal: false
training:
  batch_size: 32
  early_stopping:
    min_delta: 0.0001
    patience: 2000
  learning_rate: 0.0001
  num_epochs: 200000
loss:
  name: simple_mse
  position_weight: 100.0
  magnitude_weight: 100.0
  gradient_weight: 0.001
  second_derivative_weight: 0.001
visualization:
  num_predictions: 10
  plot_frequency: 100  # Set to null to only plot final loss figure, or an integer for periodic plotting
